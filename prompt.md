# AI 执行 Prompt：加油订单详情页高还原生成

你是一个资深移动端 UI 还原助手。请根据项目中的参考截图和 `design-skill.md`，生成一个高还原的移动端订单详情页。

## 输入材料

请优先读取：

1. `design-skill.md`
2. `references/original-screenshot.png`
3. `figma/figma-link.txt`，如果其中提供了 Figma 链接

## 输出目标

请生成或修改以下文件：

```text
output/preview.html
output/style.css
```

## 必须遵守

1. 页面宽度为 375px。
2. 页面为移动端小程序订单详情页。
3. 顶部必须是青绿色渐变状态区。
4. 顶部金额 `￥283.8` 必须最大、最醒目。
5. 右侧状态必须为 `已加油`。
6. 中部为白色圆角卡片。
7. 费用明细字段顺序不能改变。
8. 优惠金额必须为红橙色，并保留负号。
9. 实际支付金额必须与顶部金额一致。
10. 底部为固定白色操作栏，右侧按钮为绿色胶囊按钮。
11. 不得新增原图没有的文案。
12. 不得使用厚重阴影、复杂插画、营销海报式装饰。

## 还原优先级

1. 页面结构和信息层级
2. 顶部绿色渐变和金额突出
3. 卡片圆角、间距、左右对齐
4. 费用明细颜色和实际支付金额
5. 加油站图片、胶囊按钮、图标细节

## 检查要求

生成完成后，请对照 `notes/review-checklist.md` 自查，并列出你已满足的项目。
